<beginning>
<!-- Property 33: Modify the DICOM protocol version to unsupported value 0xFFFF -->
<embedded_functions><![CDATA[
    static void em_modify_dicom_version(
        const rule_info_t *rule, int verdict, uint64_t timestamp,
        uint64_t counter, const mmt_array_t * const trace ){
        int new_val = 0xFFFF;
        int is_string = 0;
        // Replace protocol version field (attribute ID 3 in protocol 701)
        replace_dicom_attribute(701, 3, &new_val, is_string);
        forward_packet();
    }
]]></embedded_functions>

<property property_id="33" type_property="FORWARD"
        description="Modify DICOM protocol version to unsupported value (0xFFFF)"
        if_satisfied="em_modify_dicom_version">
    <event description="Got any DICOM packets"
        boolean_expression="((dicom.pdu_type &gt; 0) &amp;&amp; (dicom.pdu_type &lt; 8))"/>
</property>
</beginning>