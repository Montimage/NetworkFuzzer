<beginning>
<!-- Property 41: Modify DICOM patient name in data PDVs -->
<embedded_functions><![CDATA[
static void em_modify_dicom_patient_name(
     const rule_info_t *rule, int verdict, uint64_t timestamp,
     uint64_t counter, const mmt_array_t * const trace ){
     int is_string = 1;
     char *new_val = "R????????";
     replace_dicom_attribute(701, 15, &new_val, is_string);
     forward_packet();
}
]]></embedded_functions>

<property property_id="41" type_property="FORWARD"
     description="Modify DICOM patient name"
     if_satisfied="em_modify_dicom_patient_name">
    <event description="Got DICOM P-DATA-TF data PDV (pdv_flags == 2)"
        boolean_expression="((dicom.pdu_type == 4) &amp;&amp; (dicom.pdv_flags == 2))"/>
</property>
</beginning>
