<beginning>
<!-- Property 30: Test DICOM PDU type handling by using incorrect value -->
<embedded_functions><![CDATA[
static void em_modify_dicom_pdu_type(
     const rule_info_t *rule, int verdict, uint64_t timestamp,
     uint64_t counter, const mmt_array_t * const trace ){
     int is_string = 0;
     int new_val = 5; // incorrect PDU type (A-RELEASE-RQ instead of A-ASSOCIATE-AC)
     replace_dicom_attribute(701, 1, &new_val, is_string);
     forward_packet();
}
]]></embedded_functions>

<property property_id="30" type_property="FORWARD"
     description="Modify DICOM PDU type to incorrect value on A-ASSOCIATE-AC packets"
     if_satisfied="em_modify_dicom_pdu_type">
    <event description="Got DICOM A-ASSOCIATE-AC packet (PDU type 2)"
        boolean_expression="(dicom.pdu_type == 2)"/>
</property>
</beginning>
