<beginning>
<!-- Property 30: Test DICOM PDU type handling by using incorrect or invalid values outside 0x01-0x07 range -->
<embedded_functions><![CDATA[
void on_load(){
	srand(time(NULL));
}

int em_random(int max, int min){
	return (rand() % (max-min) + min);
}
#define random em_random

static void em_modify_dicom_pdu_type(
     const rule_info_t *rule, int verdict, uint64_t timestamp,
     uint64_t counter, const mmt_array_t * const trace ){
	//int new_val = random(15, 8); // invalid PDU type
	int new_val = 5; // incorrect PDU type
     int is_string = 0;
     // modify the PDU type to invalid or incorrect value
     replace_dicom_attribute(701, 1, &new_val, is_string);
     forward_packet();
}
]]></embedded_functions>

<property property_id="30" type_property="FORWARD"
     description="Test DICOM PDU type handling with incorrect or invalid values (outside 0x01-0x07)"
     if_satisfied="em_modify_dicom_pdu_type">
    <event description="Got DICOM packet A-ASSOCIATE-AC with PDU type 2"
        boolean_expression="(dicom.pdu_type == 2)"/>
</property>
</beginning>
