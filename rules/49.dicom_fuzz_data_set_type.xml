<beginning>
<!-- Property 49: Set DICOM data set type to invalid value 0xAAAA -->
<embedded_functions><![CDATA[
static void em_fuzz_data_set_type(
     const rule_info_t *rule, int verdict, uint64_t timestamp,
     uint64_t counter, const mmt_array_t * const trace ){
     int is_string = 0;
     uint16_t new_val = 0xAAAA; // invalid data set type
     replace_dicom_attribute(701, 21, &new_val, is_string);
     forward_packet();
}
]]></embedded_functions>

<property property_id="49" type_property="FORWARD"
     description="Fuzz DICOM data set type with invalid value 0xAAAA"
     if_satisfied="em_fuzz_data_set_type">
    <event description="Got DICOM P-DATA-TF command PDV"
        boolean_expression="((dicom.pdu_type == 4) &amp;&amp; (dicom.pdv_flags == 3))"/>
</property>
</beginning>
