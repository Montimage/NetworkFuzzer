<beginning>
<!-- Property 62: DUL State Machine Confusion - Send data before association -->
<embedded_functions><![CDATA[
    static void em_data_before_assoc(
        const rule_info_t *rule, int verdict, uint64_t timestamp,
        uint64_t counter, const mmt_array_t * const trace) {
        int is_string = 0;
        int new_val = 4; // P-DATA-TF PDU type
        replace_dicom_attribute(701, 1, &new_val, is_string);
        forward_packet();
    }
]]></embedded_functions>

<property property_id="62" type_property="FORWARD"
        description="Convert A-ASSOCIATE-RQ to P-DATA-TF to send data before association"
        if_satisfied="em_data_before_assoc">
    <event description="Got DICOM A-ASSOCIATE-RQ packet (PDU type 1)"
        boolean_expression="(dicom.pdu_type == 1)"/>
</property>
</beginning>
