<beginning>
<!-- Property 53: Inject C-CANCEL command (0x0FFF) -->
<embedded_functions><![CDATA[
static void em_fuzz_command_field_cancel(
     const rule_info_t *rule, int verdict, uint64_t timestamp,
     uint64_t counter, const mmt_array_t * const trace ){
     int is_string = 0;
     uint16_t new_val = 0x0FFF; // C-CANCEL
     replace_dicom_attribute(701, 14, &new_val, is_string);
     forward_packet();
}
]]></embedded_functions>

<property property_id="53" type_property="FORWARD"
     description="Inject DICOM C-CANCEL command field"
     if_satisfied="em_fuzz_command_field_cancel">
    <event description="Got DICOM P-DATA-TF command PDV"
        boolean_expression="((dicom.pdu_type == 4) &amp;&amp; (dicom.pdv_flags == 3))"/>
</property>
</beginning>
