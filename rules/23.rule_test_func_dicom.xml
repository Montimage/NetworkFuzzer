<beginning>
<embedded_functions><![CDATA[
    static void em_modify_dicom_packet(
      const rule_info_t *rule, int verdict, uint64_t timestamp, 
      uint64_t counter, const mmt_array_t * const trace ){
        //int new_val = 6;
        const char *new_val = "MONTI";
        replace_dicom_attribute(701, 4, new_val, 1);
        //set_string_value(701, 4, new_val);
        forward_packet();
    }
]]></embedded_functions>
<property property_id="23" type_property="FORWARD" 
        description="Modify DICOM version" 
        if_satisfied="em_modify_dicom_packet">
    <event description="Identify a DICOM Assoc Request packet" 
        boolean_expression="(dicom.pdu_type == 1)"/>

</property>

</beginning>