<beginning>
<!-- Property 50: Fuzz DICOM data set type on data PDVs with random values -->
<embedded_functions><![CDATA[
    #include "fuzz_dicom.h"

    static void em_fuzz_data_set_type_null(
         const rule_info_t *rule, int verdict, uint64_t timestamp,
         uint64_t counter, const mmt_array_t * const trace ){
         fuzz_dicom_attribute(701, 21, FUZZ_RANDOM, 0);
         forward_packet();
    }
]]></embedded_functions>

<property property_id="50" type_property="FORWARD"
     description="Fuzz DICOM data set type on data PDVs with random values"
     if_satisfied="em_fuzz_data_set_type_null">
    <event description="Got DICOM P-DATA-TF data PDV"
        boolean_expression="((dicom.pdu_type == 4) &amp;&amp; (dicom.pdv_flags == 2))"/>
</property>
</beginning>
