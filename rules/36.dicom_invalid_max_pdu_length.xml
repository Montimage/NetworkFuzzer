<beginning>
<!-- Property 36: Set Max PDU Length to zero in A-ASSOCIATE-RQ -->
<embedded_functions><![CDATA[
    static void em_invalid_max_pdu_length(
        const rule_info_t *rule, int verdict, uint64_t timestamp,
        uint64_t counter, const mmt_array_t * const trace) {

        uint32_t new_length = 0;
        replace_dicom_attribute(701, 8, &new_length, 0);
        forward_packet();
    }
]]></embedded_functions>

<property property_id="36" type_property="FORWARD"
        description="Set DICOM Max PDU Length to zero in A-ASSOCIATE-RQ"
        if_satisfied="em_invalid_max_pdu_length">
    <event description="Got DICOM A-ASSOCIATE-RQ packet (PDU type 1)"
        boolean_expression="(dicom.pdu_type == 1)"/>
</property>
</beginning>
