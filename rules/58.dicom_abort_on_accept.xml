<beginning>
<!-- Property 58: Convert A-ASSOCIATE-AC to A-ABORT to prevent association establishment -->
<embedded_functions><![CDATA[
    static void em_abort_on_accept(
        const rule_info_t *rule, int verdict, uint64_t timestamp,
        uint64_t counter, const mmt_array_t * const trace) {
        int is_string = 0;
        int new_val = 7; // A-ABORT PDU type
        replace_dicom_attribute(701, 1, &new_val, is_string);
        forward_packet();
    }
]]></embedded_functions>

<property property_id="58" type_property="FORWARD"
        description="Convert A-ASSOCIATE-AC to A-ABORT to block association"
        if_satisfied="em_abort_on_accept">
    <event description="Got DICOM A-ASSOCIATE-AC packet (PDU type 2)"
        boolean_expression="(dicom.pdu_type == 2)"/>
</property>
</beginning>
