<beginning>
<!-- Property 38: Modify PDV length to mismatched value -->
<embedded_functions><![CDATA[
    static void em_modify_pdv_length(
        const rule_info_t *rule, int verdict, uint64_t timestamp,
        uint64_t counter, const mmt_array_t * const trace) {
        uint32_t new_length = 50; // mismatched PDV length
        replace_dicom_attribute(701, 10, &new_length, 0);
        forward_packet();
    }
]]></embedded_functions>

<property property_id="38" type_property="FORWARD"
        description="Modify DICOM PDV length to mismatched value (50)"
        if_satisfied="em_modify_pdv_length">
    <event description="Got DICOM P-DATA-TF packet (PDU type 4)"
        boolean_expression="(dicom.pdu_type == 4)"/>
</property>
</beginning>
