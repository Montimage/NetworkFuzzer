<beginning>
<!-- Property 69: Patient Demographics Tampering - Fuzz Patient ID -->
<embedded_functions><![CDATA[
    #include "fuzz_dicom.h"

    void on_load() {
        fuzz_dicom_load_dictionary(23, "dictionaries/patient_ids.txt");
    }

    static void em_fuzz_patient_id(
        const rule_info_t *rule, int verdict, uint64_t timestamp,
        uint64_t counter, const mmt_array_t * const trace) {
        fuzz_dicom_attribute(701, 23, FUZZ_DICTIONARY, 0);
        forward_packet();
    }
]]></embedded_functions>

<property property_id="69" type_property="FORWARD"
        description="Fuzz Patient ID in data PDVs with injection/overflow payloads"
        if_satisfied="em_fuzz_patient_id">
    <event description="Got DICOM P-DATA-TF data PDV"
        boolean_expression="((dicom.pdu_type == 4) &amp;&amp; (dicom.pdv_flags == 2))"/>
</property>
</beginning>
