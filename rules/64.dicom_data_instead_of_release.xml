<beginning>
<!-- Property 64: DUL State Machine Confusion - Send data instead of release -->
<embedded_functions><![CDATA[
    static void em_data_instead_of_release(
        const rule_info_t *rule, int verdict, uint64_t timestamp,
        uint64_t counter, const mmt_array_t * const trace) {
        int is_string = 0;
        int new_val = 4; // P-DATA-TF PDU type
        replace_dicom_attribute(701, 1, &new_val, is_string);
        forward_packet();
    }
]]></embedded_functions>

<property property_id="64" type_property="FORWARD"
        description="Convert A-RELEASE-RQ to P-DATA-TF to prevent clean release"
        if_satisfied="em_data_instead_of_release">
    <event description="Got DICOM A-RELEASE-RQ packet (PDU type 5)"
        boolean_expression="(dicom.pdu_type == 5)"/>
</property>
</beginning>
