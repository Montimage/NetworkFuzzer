# DICOM protocol support
pydicom
pynetdicom>=2.0.0

# Core dependencies
python-dotenv
pandas
numpy
scapy
pyshark

# AI/ML libraries (CPU-only versions)
gymnasium>=0.29.0
stable-baselines3

# SBI spec parsing (OpenAPI YAML)
pyyaml

# GAN for traffic generation (CPU-only)
ctgan==0.11.0

# OpenAI API
openai>=1.57.4
