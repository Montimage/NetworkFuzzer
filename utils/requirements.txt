# DICOM protocol support
pydicom
pynetdicom>=2.0.0

# Core dependencies
python-dotenv
pandas
numpy
scapy
pyshark

# PyTorch CPU-only (no CUDA, avoids ~2 GB GPU download)
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# AI/ML libraries (CPU-only versions)
gymnasium>=0.29.0
stable-baselines3

# GAN for traffic generation (CPU-only)
ctgan==0.11.0

# OpenAI API
openai<1.52.0

# MCP server
mcp>=1.0.0
